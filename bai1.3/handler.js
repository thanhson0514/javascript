/**
 * Write a program that takes a number as input and displays its square.
 * @param {number} num
 * @returns {number}  the square of number
 */
function handler(num) {
  return num * num;
}

module.exports = handler;
