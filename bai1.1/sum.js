/**
 * Write a program that takes two numbers as inputs and displays their sum
 * @param {number} a
 * @param {number} b
 * @returns sum of a and b
 */
function sum(a, b) {
    return a + b;
  }
  module.exports = sum;